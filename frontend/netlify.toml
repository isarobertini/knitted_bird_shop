[build]
  base = ""                     # repo root
  publish = "frontend/dist"      # where Vite outputs
  command = "npm run build --prefix frontend"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
